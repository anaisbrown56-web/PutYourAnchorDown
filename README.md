# PutYourAnchorDown Review App








### Quick Start
1. Run `npm install` to install dependancies
2. Create a `.env` file and follow the example `.env` template
3. Create an account with https://neon.com and create a database
4. Click 'connect' and copy the connection string as the DATABASE_URL in the `.env` file
5. Run `npm run dev` to start website
